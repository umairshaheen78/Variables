# Variables (var): containers, hold a value
# Data Types: Variable Types:
# String (str): combinations of characters (letters, numbers, or symbols), must use quotation marks
# Integer (int): whole numbers
# Characters (char): single letter, number, or symbol
# Float: numbers with decimals
# Boolean (bool): True/False
# formula:
# variableName = value
# Rules for variable names
# 1. always start with a letter
# 2. can only contain numbers and letters
# 4. if more than one word is used, use camelCase (first word is lowercase and every word after is uppercase)

firstName = "Umair" #String type

favoriteBook = "Series of Unfortunate Events"
favoriteNumber = "3"
isHungry = "True" #Boolean
favChar = 'U' #Character
